services:
  genjishimada-ocr:
    container_name: genjishimada-ocr
    build:
      context: .
    networks:
      - genji-network
    restart: unless-stopped
    resources:
        limits:
          memory: 2.5gb

networks:
  genji-network:
    external: true
